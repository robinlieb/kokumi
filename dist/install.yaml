apiVersion: v1
kind: Namespace
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    control-plane: controller-manager
  name: kokumi
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: approvals.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Approval
    listKind: ApprovalList
    plural: approvals
    shortNames:
    - appr
    singular: approval
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orderName
      name: Order
      priority: 1
      type: string
    - jsonPath: .spec.preparationRef.name
      name: Preparation
      type: string
    - jsonPath: .spec.approver.username
      name: Approver
      type: string
    - jsonPath: .spec.decision
      name: Decision
      type: string
    - jsonPath: .status.conditions[?(@.type=='Counted')].status
      name: Counted
      type: string
    - jsonPath: .status.conditions[?(@.type=='Counted')].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Approval is the Schema for the approvals API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Approval
            properties:
              approver:
                description: approver identifies who submitted this vote.
                properties:
                  email:
                    description: email is the verified email address of the approver.
                    maxLength: 254
                    type: string
                  groups:
                    description: groups are the verified OIDC groups of the approver
                      at submission time.
                    items:
                      maxLength: 253
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  issuer:
                    description: issuer is the OIDC issuer URL that authenticated
                      the approver.
                    maxLength: 2048
                    minLength: 1
                    type: string
                  subject:
                    description: |-
                      subject is the OIDC subject (sub claim). Together with issuer it is the
                      stable identity key: only the latest vote per issuer and subject counts.
                    maxLength: 255
                    minLength: 1
                    type: string
                  username:
                    description: username is the human-readable name of the approver.
                    maxLength: 253
                    type: string
                required:
                - issuer
                - subject
                type: object
              comment:
                description: comment is an optional review comment.
                maxLength: 1024
                type: string
              decision:
                description: decision is the verdict of this vote.
                enum:
                - Approve
                - Reject
                type: string
              orderName:
                description: orderName is the name of the Order the reviewed Preparation
                  belongs to.
                maxLength: 253
                minLength: 1
                type: string
              preparationRef:
                description: preparationRef references the reviewed Preparation.
                properties:
                  artifactDigest:
                    description: artifactDigest is the digest of the reviewed Preparation
                      artifact.
                    maxLength: 71
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  name:
                    description: name is the name of the Preparation in the same namespace.
                    maxLength: 253
                    minLength: 1
                    type: string
                  uid:
                    description: |-
                      uid is the UID of the Preparation. A recreated Preparation with the
                      same name does not inherit votes.
                    type: string
                required:
                - artifactDigest
                - name
                - uid
                type: object
              submittedTime:
                description: |-
                  submittedTime is when the kokumi server accepted the vote. It orders
                  votes of the same approver submitted within the same second.
                format: date-time
                type: string
            required:
            - approver
            - decision
            - orderName
            - preparationRef
            - submittedTime
            type: object
            x-kubernetes-validations:
            - message: Approval spec is immutable
              rule: self == oldSelf
          status:
            description: status defines the observed state of Approval
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Approval resource.
                  The Counted condition reports whether this vote contributes to the
                  approval gate of its Preparation.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.orderName
    - jsonPath: .spec.preparationRef.name
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kitchens.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Kitchen
    listKind: KitchenList
    plural: kitchens
    singular: kitchen
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .spec.argoCDURL
      name: Argo CD URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Kitchen is the Schema for the kitchens API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Kitchen
            properties:
              argoCDURL:
                description: |-
                  ArgoCDURL is the base URL of the Argo CD instance used to build deep
                  links on the Servings page. Empty means no links.
                type: string
                x-kubernetes-validations:
                - message: must be a valid http or https URL
                  rule: self == '' || (isURL(self) && url(self).getScheme() in ['http',
                    'https'])
              auth:
                description: |-
                  Auth configures UI/API authentication. The built-in admin account
                  (auth.adminUser) and an external OIDC provider (auth.oidc) can be enabled
                  independently; both may be active at once. When Auth is unset, no identity
                  provider is configured and the server runs without authentication.
                properties:
                  adminUser:
                    description: AdminUser configures the built-in admin account used
                      for UI login.
                    properties:
                      enabled:
                        default: true
                        description: |-
                          Enabled toggles the admin account. When false, admin login is disabled and
                          the server falls back to no authentication unless another identity
                          provider (e.g. OIDC) is configured. Defaults to true.
                        type: boolean
                      secretRef:
                        default:
                          name: kokumi-server-auth
                        description: |-
                          SecretRef points to the Secret holding the admin credentials. The Secret
                          must reside in the same namespace as the Kitchen. The recognized key is
                          "password-hash" (bcrypt hash); "username" may optionally override the
                          configured username. Defaults to "kokumi-server-auth".
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      username:
                        default: admin
                        description: Username is the login name for the admin account.
                          Defaults to "admin".
                        maxLength: 253
                        pattern: ^[^/\s]+$
                        type: string
                    type: object
                  oidc:
                    description: |-
                      OIDC configures an external OpenID Connect identity provider for UI
                      login. When set, the login page offers an SSO button alongside (or
                      instead of) the admin account, depending on whether AdminUser is also
                      enabled.
                    properties:
                      clientID:
                        description: ClientID is the OAuth2/OIDC client identifier
                          registered with the issuer.
                        type: string
                      clientSecretRef:
                        default:
                          name: kokumi-server-oidc
                        description: |-
                          ClientSecretRef points to the Secret holding the OAuth2 client secret in
                          the "client-secret" key. The Secret must reside in the same namespace as
                          the Kitchen.
                          Defaults to "kokumi-server-oidc".
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      emailClaim:
                        default: email
                        description: |-
                          EmailClaim is the ID-token claim used as the user's email for
                          ServiceAccount identity mapping (kokumi.dev/identity-email). Supports
                          dotted paths for nested claims (e.g. "user.email"). Defaults to "email".
                        maxLength: 253
                        type: string
                      groupsClaim:
                        default: groups
                        description: |-
                          GroupsClaim is the ID-token claim holding the user's group memberships
                          (a list of strings) for ServiceAccount identity mapping
                          (kokumi.dev/identity-groups). Supports dotted paths for nested claims
                          (e.g. "realm_access.roles" for Keycloak). Defaults to "groups".
                        maxLength: 253
                        type: string
                      issuerURL:
                        description: |-
                          IssuerURL is the base URL of the OIDC issuer (e.g.
                          "https://accounts.google.com"). The server discovers the authorization
                          and token endpoints from the issuer's well-known configuration.
                        type: string
                        x-kubernetes-validations:
                        - message: must be a valid http or https URL
                          rule: isURL(self) && url(self).getScheme() in ['http', 'https']
                      scopes:
                        default:
                        - openid
                        - profile
                        - email
                        description: |-
                          Scopes is the list of OAuth2 scopes requested at login.
                          Defaults to ["openid", "profile", "email"].
                        items:
                          type: string
                        maxItems: 16
                        type: array
                      usernameClaim:
                        default: email
                        description: |-
                          UsernameClaim is the ID-token claim used as the kokumi username.
                          Defaults to "email".
                        maxLength: 253
                        type: string
                    required:
                    - clientID
                    - issuerURL
                    type: object
                  tokenSigningKeySecretRef:
                    default:
                      name: kokumi-server-tokens
                    description: |-
                      TokenSigningKeySecretRef points to the Secret holding the HMAC signing
                      key (key "signing-key") used to sign all session tokens, for both admin
                      and OIDC sessions. The Secret must reside in the same namespace as the
                      Kitchen. When the referenced Secret does not exist, the controller
                      creates it with a generated random key.
                      Defaults to "kokumi-server-tokens".
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
            type: object
          status:
            description: status defines the observed state of Kitchen
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Kitchen resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: menus.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Menu
    listKind: MenuList
    plural: menus
    singular: menu
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .spec.source.oci
      name: Source
      type: string
    - jsonPath: .spec.source.version
      name: Version
      type: string
    - jsonPath: .spec.overrides.values.policy
      name: Values Policy
      priority: 1
      type: string
    - jsonPath: .spec.overrides.patches.policy
      name: Patches Policy
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Menu is the Schema for the menus API.
          A Menu is a namespace-scoped, reusable template that operators define to pin
          a source artifact, base configuration, and override constraints.
          Developers consume Menus by referencing them from Orders in the same namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Menu
            properties:
              defaults:
                description: defaults defines default values inherited by consuming
                  Orders.
                properties:
                  mode:
                    default: Manual
                    description: |-
                      mode is the default promotion mode for Orders using this Menu.
                      The Order's own spec.promotion.mode takes precedence when set;
                      inheritance applies when the Order omits spec.promotion entirely.
                    enum:
                    - Automatic
                    - Manual
                    type: string
                type: object
              overrides:
                description: overrides defines what consumers (Orders) are allowed
                  to customize.
                properties:
                  patches:
                    description: patches controls patch overrides.
                    properties:
                      allowed:
                        description: |-
                          allowed lists the patch targets and their allowed JSON paths.
                          Only used when policy is Restricted.
                        items:
                          description: AllowedPatchTarget defines which patch target
                            and JSON paths consumers may use.
                          properties:
                            paths:
                              description: |-
                                paths lists the allowed JSONPath expressions within the target
                                (e.g. ".spec.replicas").
                              items:
                                type: string
                              minItems: 1
                              type: array
                            target:
                              description: target identifies the resource that consumers
                                may patch.
                              properties:
                                kind:
                                  description: kind specifies the Kubernetes resource
                                    kind to patch
                                  type: string
                                name:
                                  description: name specifies the name of the resource
                                    to patch
                                  type: string
                                namespace:
                                  description: namespace specifies the namespace of
                                    the resource (optional, defaults to same namespace
                                    as Order)
                                  type: string
                              required:
                              - kind
                              - name
                              type: object
                          required:
                          - paths
                          - target
                          type: object
                        type: array
                      policy:
                        default: None
                        description: |-
                          policy controls how consumers may apply patches.
                          All: any patch target and path is allowed.
                          Restricted: only the targets and paths listed in allowed are permitted.
                          None: no patch overrides are permitted.
                        enum:
                        - All
                        - Restricted
                        - None
                        type: string
                    required:
                    - policy
                    type: object
                    x-kubernetes-validations:
                    - message: allowed must not be empty when policy is Restricted
                      rule: self.policy != 'Restricted' || (has(self.allowed) && size(self.allowed)
                        > 0)
                  values:
                    description: values controls Helm value overrides.
                    properties:
                      allowed:
                        description: |-
                          allowed lists the dot-separated Helm value paths that consumers may set
                          (e.g. "ui.message", "replicaCount"). Only used when policy is Restricted.
                        items:
                          type: string
                        type: array
                      policy:
                        default: None
                        description: |-
                          policy controls how consumers may override Helm values.
                          All: any value path is allowed.
                          Restricted: only paths listed in allowed are permitted.
                          None: no value overrides are permitted.
                        enum:
                        - All
                        - Restricted
                        - None
                        type: string
                    required:
                    - policy
                    type: object
                    x-kubernetes-validations:
                    - message: allowed must not be empty when policy is Restricted
                      rule: self.policy != 'Restricted' || (has(self.allowed) && size(self.allowed)
                        > 0)
                required:
                - patches
                - values
                type: object
              patches:
                description: |-
                  patches defines base patches always applied to consuming Orders.
                  These are merged with (and may be overridden by) consumer patches
                  when the override policy allows it.
                items:
                  description: Patch defines a modification to apply to a resource
                  properties:
                    set:
                      additionalProperties:
                        type: string
                      description: |-
                        set contains JSONPath expressions and their values to set
                        Keys are JSONPath expressions (e.g., ".spec.replicas")
                        Values are the desired values for those paths
                      type: object
                    target:
                      description: target identifies which resource to patch
                      properties:
                        kind:
                          description: kind specifies the Kubernetes resource kind
                            to patch
                          type: string
                        name:
                          description: name specifies the name of the resource to
                            patch
                          type: string
                        namespace:
                          description: namespace specifies the namespace of the resource
                            (optional, defaults to same namespace as Order)
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                  required:
                  - set
                  - target
                  type: object
                type: array
              render:
                description: |-
                  render defines optional rendering configuration for the source artifact.
                  When absent the source is treated as a pre-rendered manifest bundle.
                  Consumers cannot change the render type.
                properties:
                  helm:
                    description: |-
                      helm renders the source OCI artifact as a Helm chart.
                      When set, the source must be a Helm chart in OCI format.
                    properties:
                      includeCRDs:
                        default: false
                        description: |-
                          includeCRDs controls whether CRDs are included in the rendered output.
                          Equivalent to helm template --include-crds.
                        type: boolean
                      namespace:
                        description: |-
                          namespace is the target namespace passed to helm template --namespace.
                          Defaults to the Order's metadata.namespace when omitted.
                        type: string
                      releaseName:
                        description: |-
                          releaseName is the Helm release name passed to helm template.
                          Defaults to the Order's metadata.name when omitted.
                        type: string
                      values:
                        description: |-
                          values holds inline Helm values merged last (highest priority).
                          Equivalent to a final -f values.yaml pass.
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  manifest:
                    description: manifest configures rendering of a raw manifest bundle
                      source.
                    properties:
                      layout:
                        default: Single
                        description: |-
                          layout controls whether the artifact's YAML files are merged into a
                          single manifest.yaml (Single) or kept as separate files (Multi).
                          Artifacts containing a kustomization.yaml are never merged.
                        enum:
                        - Single
                        - Multi
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: helm and manifest are mutually exclusive
                  rule: '!(has(self.helm) && has(self.manifest))'
              source:
                description: |-
                  source defines the immutable OCI artifact that consuming Orders will use.
                  Consumers cannot change the source or version.
                properties:
                  oci:
                    description: |-
                      oci is the full OCI URL for the source manifests.
                      Mutually exclusive with pantryRef.
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid OCI URL
                      rule: self == '' || (isURL(self) && url(self).getScheme() ==
                        'oci')
                  pantryRef:
                    description: |-
                      pantryRef references a Pantry resource whose URL is used as the source.
                      Mutually exclusive with oci.
                    properties:
                      name:
                        description: name is the name of the Pantry resource.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: |-
                      version is the semantic version or tag of the artifact
                      The controller will resolve this to a digest
                    type: string
                required:
                - version
                type: object
                x-kubernetes-validations:
                - message: exactly one of oci or pantryRef must be set
                  rule: (has(self.oci) && !has(self.pantryRef)) || (!has(self.oci)
                    && has(self.pantryRef))
              vendor:
                description: |-
                  vendor optionally configures copying the source artifact to another OCI
                  registry. When set, consuming Orders use the vendored copy.
                properties:
                  destination:
                    description: destination is the registry the artifact is published
                      to.
                    properties:
                      oci:
                        description: |-
                          oci is the full OCI URL the vendored artifact is pushed to.
                          Mutually exclusive with pantryRef.
                        type: string
                        x-kubernetes-validations:
                        - message: must be a valid OCI URL
                          rule: self == '' || (isURL(self) && url(self).getScheme()
                            == 'oci')
                      pantryRef:
                        description: |-
                          pantryRef references a Pantry resource whose URL is used as the
                          vendored destination. Mutually exclusive with oci.
                        properties:
                          name:
                            description: name is the name of the Pantry resource.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: oci and pantryRef are mutually exclusive
                      rule: '!(has(self.oci) && has(self.pantryRef))'
                  mode:
                    default: Render
                    description: |-
                      mode controls what is published to the destination.
                      Render (default): the source is rendered per spec.render and spec.patches
                      are applied before pushing the resulting manifests.
                      Copy: the raw source artifact is pushed unchanged.
                    enum:
                    - Render
                    - Copy
                    type: string
                required:
                - destination
                type: object
                x-kubernetes-validations:
                - message: mode Render requires a destination
                  rule: '!(has(self.mode) && self.mode == ''Render'' && !has(self.destination))'
            required:
            - overrides
            - source
            type: object
          status:
            description: status defines the observed state of Menu
            properties:
              conditions:
                description: conditions represent the current state of the Menu resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: |-
                  configHash is a SHA-256 hash of the resolved source and destination OCI
                  URLs plus version, render, and patches. When it matches the current spec
                  inputs the previously published artifact is reused without re-rendering.
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              source:
                description: |-
                  source is the consumable source address for Orders. Absent until the
                  Menu controller has resolved (and, if configured, vendored) the source.
                properties:
                  digest:
                    description: |-
                      digest is the resolved digest of the artifact. Empty when resolution
                      was best-effort and the registry could not be reached.
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  oci:
                    description: oci is the full OCI URL of the consumable artifact.
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid OCI URL
                      rule: isURL(self) && url(self).getScheme() == 'oci'
                  pantryRef:
                    description: |-
                      pantryRef references the Pantry providing pull credentials for oci.
                      Absent when the registry is anonymously accessible.
                    properties:
                      name:
                        description: name is the name of the Pantry resource.
                        type: string
                    required:
                    - name
                    type: object
                  preRendered:
                    description: |-
                      preRendered reports whether the advertised artifact is a rendered,
                      patched manifest bundle produced by the Menu (vendor mode Render).
                      When true, Orders treat the artifact as a plain manifest base and
                      cannot apply Helm value overrides.
                    type: boolean
                  version:
                    description: version is the tag of the consumable artifact.
                    minLength: 1
                    type: string
                required:
                - oci
                - version
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: orders.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Order
    listKind: OrderList
    plural: orders
    singular: order
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.latestPreparationName
      name: Latest Preparation
      type: string
    - jsonPath: .spec.menuRef.name
      name: Menu
      priority: 1
      type: string
    - jsonPath: .spec.source.oci
      name: Source
      priority: 1
      type: string
    - jsonPath: .spec.source.version
      name: Version
      priority: 1
      type: string
    - jsonPath: .spec.promotion.mode
      name: Promotion
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Order is the Schema for the orders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Order
            properties:
              destination:
                description: |-
                  destination defines where the rendered Preparation artifact will be pushed.
                  When omitted, the in-cluster registry is used automatically.
                properties:
                  oci:
                    description: oci is the OCI registry URL where configured manifests
                      will be pushed
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid OCI URL
                      rule: self == '' || (isURL(self) && url(self).getScheme() ==
                        'oci')
                  pantryRef:
                    description: |-
                      pantryRef references a Pantry resource whose URL is used as the destination.
                      Mutually exclusive with oci.
                    properties:
                      name:
                        description: name is the name of the Pantry resource.
                        type: string
                    required:
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: oci and pantryRef are mutually exclusive
                  rule: '!(has(self.oci) && has(self.pantryRef))'
              edits:
                description: |-
                  edits defines UI-driven field modifications applied after patches.
                  Unlike patches, edits are never inherited from a Menu and are intended
                  for interactive changes made through the web UI.
                  When menuRef is set, edits are still subject to the Menu's patch override policy.
                items:
                  description: Patch defines a modification to apply to a resource
                  properties:
                    set:
                      additionalProperties:
                        type: string
                      description: |-
                        set contains JSONPath expressions and their values to set
                        Keys are JSONPath expressions (e.g., ".spec.replicas")
                        Values are the desired values for those paths
                      type: object
                    target:
                      description: target identifies which resource to patch
                      properties:
                        kind:
                          description: kind specifies the Kubernetes resource kind
                            to patch
                          type: string
                        name:
                          description: name specifies the name of the resource to
                            patch
                          type: string
                        namespace:
                          description: namespace specifies the namespace of the resource
                            (optional, defaults to same namespace as Order)
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                  required:
                  - set
                  - target
                  type: object
                type: array
              menuRef:
                description: |-
                  menuRef references a Menu in the Order's namespace that provides the
                  source, base configuration, and override constraints for this Order.
                  Must not be set when source is used.
                properties:
                  name:
                    description: |-
                      name is the name of the Menu to use as a template. The Menu must exist
                      in the same namespace as the Order.
                    type: string
                required:
                - name
                type: object
              patches:
                description: |-
                  patches defines deterministic transformations applied to the source artifact
                  before producing a Preparation.
                  When menuRef is set, these patches are subject to the Menu's override policy.
                items:
                  description: Patch defines a modification to apply to a resource
                  properties:
                    set:
                      additionalProperties:
                        type: string
                      description: |-
                        set contains JSONPath expressions and their values to set
                        Keys are JSONPath expressions (e.g., ".spec.replicas")
                        Values are the desired values for those paths
                      type: object
                    target:
                      description: target identifies which resource to patch
                      properties:
                        kind:
                          description: kind specifies the Kubernetes resource kind
                            to patch
                          type: string
                        name:
                          description: name specifies the name of the resource to
                            patch
                          type: string
                        namespace:
                          description: namespace specifies the namespace of the resource
                            (optional, defaults to same namespace as Order)
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                  required:
                  - set
                  - target
                  type: object
                type: array
              promotion:
                description: promotion configures how new Preparations become active
                  Servings.
                properties:
                  approvals:
                    description: |-
                      approvals configures an approval gate on the promoted artifact. When
                      set, the Preparation is blocked from being served until enough
                      distinct approvers from the listed groups have approved it, in both
                      Automatic and Manual mode. When omitted, no approval gate applies.
                      The policy is copied into each Preparation when it is created, so a
                      change produces a new Preparation that needs fresh approvals.
                    properties:
                      allowedGroups:
                        description: allowedGroups lists the groups whose members
                          are eligible to approve.
                        items:
                          maxLength: 253
                          type: string
                        maxItems: 16
                        minItems: 1
                        type: array
                        x-kubernetes-list-type: set
                      requiredApprovals:
                        description: requiredApprovals is the minimum number of distinct
                          eligible approvers.
                        format: int32
                        maximum: 32
                        minimum: 1
                        type: integer
                    required:
                    - allowedGroups
                    - requiredApprovals
                    type: object
                  mode:
                    default: Manual
                    description: |-
                      mode controls how a newly created Preparation becomes the active Serving.
                      Automatic promotes it immediately; Manual requires explicit promotion.
                    enum:
                    - Automatic
                    - Manual
                    type: string
                type: object
              render:
                description: |-
                  render defines optional rendering configuration for the source artifact.
                  When absent the source OCI artifact is treated as a pre-rendered manifest bundle.
                  When render.helm is set the source must be a Helm chart in OCI format.
                  When menuRef is set, the render type is inherited from the Menu and this
                  field is used only for consumer value overrides (render.helm.values).
                properties:
                  helm:
                    description: |-
                      helm renders the source OCI artifact as a Helm chart.
                      When set, the source must be a Helm chart in OCI format.
                    properties:
                      includeCRDs:
                        default: false
                        description: |-
                          includeCRDs controls whether CRDs are included in the rendered output.
                          Equivalent to helm template --include-crds.
                        type: boolean
                      namespace:
                        description: |-
                          namespace is the target namespace passed to helm template --namespace.
                          Defaults to the Order's metadata.namespace when omitted.
                        type: string
                      releaseName:
                        description: |-
                          releaseName is the Helm release name passed to helm template.
                          Defaults to the Order's metadata.name when omitted.
                        type: string
                      values:
                        description: |-
                          values holds inline Helm values merged last (highest priority).
                          Equivalent to a final -f values.yaml pass.
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                  manifest:
                    description: manifest configures rendering of a raw manifest bundle
                      source.
                    properties:
                      layout:
                        default: Single
                        description: |-
                          layout controls whether the artifact's YAML files are merged into a
                          single manifest.yaml (Single) or kept as separate files (Multi).
                          Artifacts containing a kustomization.yaml are never merged.
                        enum:
                        - Single
                        - Multi
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: helm and manifest are mutually exclusive
                  rule: '!(has(self.helm) && has(self.manifest))'
              source:
                description: |-
                  source defines the immutable base artifact to render from.
                  Must not be set when menuRef is used.
                properties:
                  oci:
                    description: |-
                      oci is the full OCI URL for the source manifests.
                      Mutually exclusive with pantryRef.
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid OCI URL
                      rule: self == '' || (isURL(self) && url(self).getScheme() ==
                        'oci')
                  pantryRef:
                    description: |-
                      pantryRef references a Pantry resource whose URL is used as the source.
                      Mutually exclusive with oci.
                    properties:
                      name:
                        description: name is the name of the Pantry resource.
                        type: string
                    required:
                    - name
                    type: object
                  version:
                    description: |-
                      version is the semantic version or tag of the artifact
                      The controller will resolve this to a digest
                    type: string
                required:
                - version
                type: object
                x-kubernetes-validations:
                - message: exactly one of oci or pantryRef must be set
                  rule: (has(self.oci) && !has(self.pantryRef)) || (!has(self.oci)
                    && has(self.pantryRef))
            type: object
            x-kubernetes-validations:
            - message: exactly one of source or menuRef must be set
              rule: (has(self.source) && !has(self.menuRef)) || (!has(self.source)
                && has(self.menuRef))
          status:
            description: status defines the observed state of Order
            properties:
              conditions:
                description: conditions represent the current state of the Order resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              latestArtifactDigest:
                description: |-
                  latestArtifactDigest is the SHA256 digest of the OCI artifact produced by the
                  most recent Preparation. It is used as the pointer for the parentDigest of
                  the next Preparation.
                pattern: ^sha256:[a-f0-9]{64}$
                type: string
              latestConfigHash:
                description: |-
                  latestConfigHash is a SHA-256 hash of the resolved source and destination
                  OCI URLs plus version, menuRef, render, patches, and edits.
                type: string
              latestPreparationName:
                description: |-
                  latestPreparationName is the name of the most recent immutable Preparation
                  produced from this Order
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: pantries.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Pantry
    listKind: PantryList
    plural: pantries
    singular: pantry
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Pantry is the Schema for the pantries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Pantry
            properties:
              description:
                description: description is a human-readable description of this Pantry.
                type: string
              secretRef:
                description: |-
                  secretRef references a kubernetes.io/dockerconfigjson Secret in the same
                  namespace that holds the registry credentials.
                  When omitted the registry is accessed anonymously.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              url:
                description: url is the full OCI URL for this Pantry, including the
                  path to the artifact.
                type: string
                x-kubernetes-validations:
                - message: must be a valid OCI URL
                  rule: isURL(self) && url(self).getScheme() == 'oci'
            required:
            - url
            type: object
          status:
            description: status defines the observed state of Pantry
            properties:
              conditions:
                description: conditions represent the current state of the Pantry
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: preparations.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Preparation
    listKind: PreparationList
    plural: preparations
    shortNames:
    - prep
    singular: preparation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orderName
      name: Order
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Approved')].reason
      name: Approved
      type: string
    - jsonPath: .spec.artifact.digest
      name: Digest
      priority: 1
      type: string
    - jsonPath: .spec.artifact.signed
      name: Signed
      priority: 1
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Preparation is the Schema for the preparations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Preparation
            properties:
              approvalPolicy:
                description: |-
                  approvalPolicy is the approval gate copied from the Order when this
                  Preparation was created. When set, the Preparation is only served once
                  the policy is satisfied. It is also recorded on the OCI artifact.
                properties:
                  allowedGroups:
                    description: allowedGroups lists the groups whose members are
                      eligible to approve.
                    items:
                      maxLength: 253
                      type: string
                    maxItems: 16
                    minItems: 1
                    type: array
                    x-kubernetes-list-type: set
                  requiredApprovals:
                    description: requiredApprovals is the minimum number of distinct
                      eligible approvers.
                    format: int32
                    maximum: 32
                    minimum: 1
                    type: integer
                required:
                - allowedGroups
                - requiredApprovals
                type: object
              artifact:
                description: artifact defines the output artifact information
                properties:
                  digest:
                    description: digest is the SHA256 digest of the artifact
                    maxLength: 71
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  ociRef:
                    description: ociRef is the full OCI reference including digest
                    maxLength: 2048
                    pattern: ^oci://.*@sha256:[a-f0-9]{64}$
                    type: string
                  signed:
                    description: signed indicates whether the artifact has been cryptographically
                      signed
                    type: boolean
                required:
                - digest
                - ociRef
                type: object
              commitMessage:
                description: |-
                  commitMessage is the human-readable description of why this Preparation was created.
                  It is stored as the org.opencontainers.image.description annotation on the OCI artifact.
                maxLength: 512
                type: string
              configHash:
                description: configHash is the SHA256 hash of the canonicalized order
                  configuration
                maxLength: 71
                pattern: ^sha256:[a-f0-9]+$
                type: string
              gitSource:
                description: |-
                  gitSource records the SCM provenance of the base artifact that produced
                  this Preparation. Empty when the base artifact carries no git provenance.
                properties:
                  commitHash:
                    description: |-
                      commitHash is the SCM commit SHA of the base artifact's source files
                      (org.opencontainers.image.revision). Empty when the base artifact carries
                      no git revision.
                    maxLength: 64
                    type: string
                  repo:
                    description: |-
                      repo is the SCM repository URL of the base artifact's source
                      (org.opencontainers.image.source). Empty when the base artifact carries
                      no git provenance.
                    maxLength: 2048
                    type: string
                  tag:
                    description: |-
                      tag is the SCM tag of the base artifact's source files
                      (org.opencontainers.image.version). Empty when the base artifact carries
                      no git tag.
                    maxLength: 256
                    type: string
                type: object
              orderName:
                description: orderName is the name of the Order this preparation belongs
                  to
                maxLength: 253
                type: string
              parentDigest:
                description: |-
                  parentDigest is the SHA256 digest of the artifact produced by the immediately preceding
                  Preparation for this Order. It is empty for the first Preparation and is stored as the
                  kokumi.dev/parent annotation on the OCI artifact, forming a git-like revision chain.
                maxLength: 71
                pattern: ^sha256:[a-f0-9]{64}$
                type: string
              renderer:
                description: renderer defines the renderer used to process this preparation
                properties:
                  digest:
                    description: digest is the SHA256 digest of the renderer binary/image
                    maxLength: 71
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  renderType:
                    description: |-
                      renderType records how the source was rendered to produce this Preparation.
                      One of "Manifest" or "Helm".
                    enum:
                    - Manifest
                    - Helm
                    maxLength: 8
                    type: string
                  version:
                    description: version is the semantic version of the renderer
                    maxLength: 64
                    type: string
                required:
                - digest
                - renderType
                - version
                type: object
              source:
                description: source defines the source artifact information
                properties:
                  baseDigest:
                    description: baseDigest is the SHA256 digest of the base source
                      artifact
                    maxLength: 71
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  oci:
                    description: oci is the OCI registry URL for the source manifests
                    maxLength: 2048
                    type: string
                    x-kubernetes-validations:
                    - message: must be a valid OCI URL
                      rule: isURL(self) && url(self).getScheme() == 'oci'
                required:
                - baseDigest
                - oci
                type: object
            required:
            - artifact
            - configHash
            - orderName
            - renderer
            - source
            type: object
            x-kubernetes-validations:
            - message: Preparation spec is immutable
              rule: self == oldSelf
          status:
            description: status defines the observed state of Preparation
            properties:
              approval:
                description: |-
                  approval aggregates the Approvals of this Preparation. Only set when
                  spec.approvalPolicy is set.
                properties:
                  approvedCount:
                    description: approvedCount is the number of eligible approvers
                      whose latest vote is Approve.
                    format: int32
                    type: integer
                  attestation:
                    description: attestation references the OCI artifact recording
                      the sealed votes.
                    properties:
                      digest:
                        description: digest is the manifest digest of the attestation.
                        maxLength: 71
                        pattern: ^sha256:[a-f0-9]{64}$
                        type: string
                      ociRef:
                        description: ociRef is the full OCI reference of the attestation
                          including digest.
                        maxLength: 2048
                        type: string
                    required:
                    - digest
                    - ociRef
                    type: object
                  ineligibleCount:
                    description: ineligibleCount is the number of approvers not in
                      any allowed group.
                    format: int32
                    type: integer
                  rejectedCount:
                    description: rejectedCount is the number of eligible approvers
                      whose latest vote is Reject.
                    format: int32
                    type: integer
                  requiredApprovals:
                    description: requiredApprovals is the number of approvals required
                      by the policy.
                    format: int32
                    type: integer
                  sealedTime:
                    description: |-
                      sealedTime is when the votes were locked because the Preparation was
                      promoted. Votes submitted afterwards are ignored.
                    format: date-time
                    type: string
                  submissionCount:
                    description: |-
                      submissionCount is the total number of Approvals for this Preparation,
                      including superseded ones.
                    format: int32
                    type: integer
                  votes:
                    description: |-
                      votes lists the latest vote of each approver. Once sealed, only these
                      votes are considered.
                    items:
                      description: ApprovalVote is the latest vote of a single approver
                        on a Preparation.
                      properties:
                        approvalName:
                          description: approvalName is the name of the Approval carrying
                            this vote.
                          maxLength: 253
                          type: string
                        decision:
                          description: decision is the verdict of the vote.
                          enum:
                          - Approve
                          - Reject
                          type: string
                        issuer:
                          description: issuer is the OIDC issuer of the approver.
                          maxLength: 2048
                          type: string
                        result:
                          description: result reports whether the vote counts towards
                            the gate.
                          enum:
                          - Counted
                          - NotEligible
                          type: string
                        subject:
                          description: subject is the OIDC subject of the approver.
                          maxLength: 255
                          type: string
                        submittedTime:
                          description: submittedTime is when the vote was submitted.
                          format: date-time
                          type: string
                        username:
                          description: username is the human-readable name of the
                            approver.
                          maxLength: 253
                          type: string
                      required:
                      - approvalName
                      - decision
                      - issuer
                      - result
                      - subject
                      - submittedTime
                      type: object
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              conditions:
                description: conditions represent the current state of the Preparation
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.orderName
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: recipes.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Recipe
    listKind: RecipeList
    plural: recipes
    singular: recipe
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Recipe is the Schema for the recipes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Recipe
            properties:
              foo:
                description: foo is an example field of Recipe. Edit recipe_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: status defines the observed state of Recipe
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Recipe resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: servings.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Serving
    listKind: ServingList
    plural: servings
    singular: serving
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orderName
      name: Order
      type: string
    - jsonPath: .spec.preparationName
      name: Preparation
      priority: 1
      type: string
    - jsonPath: .status.targetPreparationName
      name: Target
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[?(@.type=='Approved')].reason
      name: Approved
      type: string
    - jsonPath: .status.observedPreparationName
      name: Observed
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Serving is the Schema for the servings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Serving
            properties:
              orderName:
                description: orderName is the name of the Order to serve
                type: string
              preparationName:
                description: |-
                  preparationName is the Preparation promoted for Manual promotion.
                  It is ignored when the Order uses Automatic promotion.
                type: string
            required:
            - orderName
            type: object
          status:
            description: status defines the observed state of Serving
            properties:
              conditions:
                description: conditions represent the current state of the Serving
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployedDigest:
                description: deployedDigest is the SHA256 digest of the currently
                  deployed artifact
                pattern: ^sha256:[a-f0-9]{64}$
                type: string
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              observedPreparationName:
                description: observedPreparationName is the name of the Preparation
                  that was last observed by the controller
                type: string
              targetPreparationName:
                description: |-
                  targetPreparationName is the Preparation the Serving is converging to:
                  spec.preparationName for Manual promotion, or the newest Ready
                  Preparation of the Order for Automatic promotion.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    kokumi.dev/description: Full access; used by the built-in admin login
    kokumi.dev/identity-groups: kokumi-admin
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-admin
  namespace: kokumi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-controller-manager
  namespace: kokumi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    kokumi.dev/description: Create/update/delete kokumi resources
    kokumi.dev/identity-groups: kokumi-editor
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-editor
  namespace: kokumi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-server
  namespace: kokumi
---
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    kokumi.dev/description: Read-only access
    kokumi.dev/identity-groups: kokumi-viewer
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-viewer
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-leader-election-role
  namespace: kokumi
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kokumi-server-auth-reader
  namespace: kokumi
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - watch
  - list
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kokumi-server-impersonator
  namespace: kokumi
rules:
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - impersonate
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-approval-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-approval-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-approval-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-kitchen-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-kitchen-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-kitchen-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - get
  - list
  - watch
- apiGroups:
  - argoproj.io
  resources:
  - applications
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals/status
  - kitchens/status
  - menus/status
  - orders/status
  - pantries/status
  - preparations/status
  - recipes/status
  - servings/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens
  - menus
  - orders
  - pantries
  - preparations
  - recipes
  - servings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens/finalizers
  - menus/finalizers
  - orders/finalizers
  - pantries/finalizers
  - preparations/finalizers
  - recipes/finalizers
  - servings/finalizers
  verbs:
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-menu-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-menu-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-menu-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-metrics-auth-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-order-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-order-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-order-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - orders/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-pantry-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-pantry-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-pantry-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - pantries/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-preparation-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-preparation-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-preparation-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-recipe-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-recipe-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-recipe-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - recipes/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-server-approval-submitter
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-server-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  - orders
  - preparations
  - recipes
  - servings
  - pantries
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations
  verbs:
  - approve
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-server-kitchen-watcher
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - kitchens
  - menus
  - orders
  - preparations
  - recipes
  - servings
  - pantries
  - approvals
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-server-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  - orders
  - preparations
  - recipes
  - servings
  - pantries
  - kitchens
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - approvals
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - preparations
  verbs:
  - approve
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kokumi-server-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - menus
  - orders
  - preparations
  - recipes
  - servings
  - pantries
  - kitchens
  - approvals
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-serving-admin-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings
  verbs:
  - '*'
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-serving-editor-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-serving-viewer-role
rules:
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - delivery.kokumi.dev
  resources:
  - servings/status
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-leader-election-rolebinding
  namespace: kokumi
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kokumi-leader-election-role
subjects:
- kind: ServiceAccount
  name: kokumi-controller-manager
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kokumi-server-auth-reader-binding
  namespace: kokumi
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kokumi-server-auth-reader
subjects:
- kind: ServiceAccount
  name: kokumi-server
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: kokumi-server-impersonator-binding
  namespace: kokumi
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: kokumi-server-impersonator
subjects:
- kind: ServiceAccount
  name: kokumi-server
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
  name: kokumi-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-manager-role
subjects:
- kind: ServiceAccount
  name: kokumi-controller-manager
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: kokumi-controller-manager
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-server-admin-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-server-role
subjects:
- kind: ServiceAccount
  name: kokumi-admin
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-server-approval-submitter-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-server-approval-submitter
subjects:
- kind: ServiceAccount
  name: kokumi-server
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-server-editor-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-server-editor-role
subjects:
- kind: ServiceAccount
  name: kokumi-editor
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-server-kitchen-watcher-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-server-kitchen-watcher
subjects:
- kind: ServiceAccount
  name: kokumi-server
  namespace: kokumi
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kokumi-server-viewer-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kokumi-server-viewer-role
subjects:
- kind: ServiceAccount
  name: kokumi-viewer
  namespace: kokumi
---
apiVersion: v1
data:
  config.yml: |
    version: 0.1
    log:
      level: info

    http:
      addr: :5000

    storage:
      filesystem:
        rootdirectory: /var/lib/registry
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/component: registry
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    app.kubernetes.io/part-of: kokumi
  name: kokumi-registry-config
  namespace: kokumi
---
apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-server-auth
  namespace: kokumi
stringData:
  password-hash: $2y$05$MG5FZ/WlMewHp8kwaoZixeQ8NCXjhp7ZWwx1N40pQ6oc3VfL9Xu0y
  username: admin
type: Opaque
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    control-plane: controller-manager
  name: kokumi-controller-manager-metrics-service
  namespace: kokumi
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    app.kubernetes.io/name: kokumi
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: registry
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    app.kubernetes.io/part-of: kokumi
  name: kokumi-registry
  namespace: kokumi
spec:
  ports:
  - name: http
    nodePort: 30050
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: registry
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-server
  namespace: kokumi
spec:
  ports:
  - name: http
    nodePort: 30051
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  type: NodePort
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/component: registry
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    app.kubernetes.io/part-of: kokumi
  name: kokumi-registry-pvc
  namespace: kokumi
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    control-plane: controller-manager
  name: kokumi-controller-manager
  namespace: kokumi
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kokumi
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/name: kokumi
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --metrics-bind-address=:8443
        - --leader-elect
        - --health-probe-bind-address=:8081
        command:
        - /manager
        image: ghcr.io/kokumi-dev/kokumi:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports: []
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: kokumi-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: registry
    app.kubernetes.io/managed-by: kustomize
    app.kubernetes.io/name: kokumi
    app.kubernetes.io/part-of: kokumi
  name: kokumi-registry
  namespace: kokumi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: registry
  template:
    metadata:
      labels:
        app: registry
    spec:
      containers:
      - image: registry:3
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        name: registry
        ports:
        - containerPort: 5000
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - mountPath: /etc/docker/registry
          name: config
        - mountPath: /var/lib/registry
          name: storage
      volumes:
      - configMap:
          name: kokumi-registry-config
        name: config
      - name: storage
        persistentVolumeClaim:
          claimName: kokumi-registry-pvc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: kokumi
  name: kokumi-server
  namespace: kokumi
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: server
      app.kubernetes.io/name: kokumi
  template:
    metadata:
      labels:
        app.kubernetes.io/component: server
        app.kubernetes.io/name: kokumi
    spec:
      containers:
      - args:
        - --bind-address=:8080
        command:
        - /server
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: ghcr.io/kokumi-dev/kokumi:latest
        imagePullPolicy: IfNotPresent
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        name: server
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: kokumi-server
      volumes:
      - emptyDir: {}
        name: tmp
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: kokumi-approval-integrity
  namespace: kokumi
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - delivery.kokumi.dev
      apiVersions:
      - '*'
      operations:
      - CREATE
      - UPDATE
      - DELETE
      resources:
      - approvals
  validations:
  - expression: request.operation != 'CREATE' || request.userInfo.username == variables.serverSA
    message: Approvals can only be submitted through the kokumi server
    reason: Forbidden
  - expression: request.operation != 'UPDATE'
    message: Approvals are immutable
    reason: Forbidden
  - expression: request.operation != 'DELETE' || request.userInfo.username == variables.namespaceController
    message: Approvals are append-only and cannot be deleted
    reason: Forbidden
  variables:
  - expression: '''system:serviceaccount:kokumi:kokumi-server'''
    name: serverSA
  - expression: '''system:serviceaccount:kube-system:namespace-controller'''
    name: namespaceController
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: kokumi-status-integrity
  namespace: kokumi
spec:
  failurePolicy: Fail
  matchConstraints:
    resourceRules:
    - apiGroups:
      - delivery.kokumi.dev
      apiVersions:
      - '*'
      operations:
      - UPDATE
      resources:
      - approvals/status
      - preparations/status
      - servings/status
  validations:
  - expression: request.userInfo.username == 'system:serviceaccount:kokumi:kokumi-controller-manager'
    message: Approval, Preparation and Serving status is owned by the kokumi controller
    reason: Forbidden
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: kokumi-approval-integrity
  namespace: kokumi
spec:
  policyName: kokumi-approval-integrity
  validationActions:
  - Deny
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: kokumi-status-integrity
  namespace: kokumi
spec:
  policyName: kokumi-status-integrity
  validationActions:
  - Deny
