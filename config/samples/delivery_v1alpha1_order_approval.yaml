apiVersion: delivery.kokumi.dev/v1alpha1
kind: Order
metadata:
  labels:
    app.kubernetes.io/name: kokumi
    app.kubernetes.io/managed-by: kustomize
  name: external-secrets-approval
spec:
  source:
    oci: oci://ghcr.io/kokumi-dev/testdata/external-secrets
    version: "0.1.0"

  patches:
    - target:
        kind: Deployment
        name: external-secrets-webhook
      set:
        .spec.replicas: "2"

  destination:
    oci: oci://kokumi-registry.kokumi.svc.cluster.local:5000/preparation/external-secrets-approval

  promotion:
    mode: Manual
    # Two members of release-approvers (see hack/oidc/dex-config.yaml) must
    # approve each Preparation in the UI before it can be promoted.
    approvals:
      requiredApprovals: 2
      allowedGroups:
      - release-approvers
