---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: approvals.delivery.kokumi.dev
spec:
  group: delivery.kokumi.dev
  names:
    kind: Approval
    listKind: ApprovalList
    plural: approvals
    shortNames:
    - appr
    singular: approval
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.orderName
      name: Order
      priority: 1
      type: string
    - jsonPath: .spec.preparationRef.name
      name: Preparation
      type: string
    - jsonPath: .spec.approver.username
      name: Approver
      type: string
    - jsonPath: .spec.decision
      name: Decision
      type: string
    - jsonPath: .status.conditions[?(@.type=='Counted')].status
      name: Counted
      type: string
    - jsonPath: .status.conditions[?(@.type=='Counted')].reason
      name: Reason
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Approval is the Schema for the approvals API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Approval
            properties:
              approver:
                description: approver identifies who submitted this vote.
                properties:
                  email:
                    description: email is the verified email address of the approver.
                    maxLength: 254
                    type: string
                  groups:
                    description: groups are the verified OIDC groups of the approver
                      at submission time.
                    items:
                      maxLength: 253
                      type: string
                    maxItems: 64
                    type: array
                    x-kubernetes-list-type: set
                  issuer:
                    description: issuer is the OIDC issuer URL that authenticated
                      the approver.
                    maxLength: 2048
                    minLength: 1
                    type: string
                  subject:
                    description: |-
                      subject is the OIDC subject (sub claim). Together with issuer it is the
                      stable identity key: only the latest vote per issuer and subject counts.
                    maxLength: 255
                    minLength: 1
                    type: string
                  username:
                    description: username is the human-readable name of the approver.
                    maxLength: 253
                    type: string
                required:
                - issuer
                - subject
                type: object
              comment:
                description: comment is an optional review comment.
                maxLength: 1024
                type: string
              decision:
                description: decision is the verdict of this vote.
                enum:
                - Approve
                - Reject
                type: string
              orderName:
                description: orderName is the name of the Order the reviewed Preparation
                  belongs to.
                maxLength: 253
                minLength: 1
                type: string
              preparationRef:
                description: preparationRef references the reviewed Preparation.
                properties:
                  artifactDigest:
                    description: artifactDigest is the digest of the reviewed Preparation
                      artifact.
                    maxLength: 71
                    pattern: ^sha256:[a-f0-9]{64}$
                    type: string
                  name:
                    description: name is the name of the Preparation in the same namespace.
                    maxLength: 253
                    minLength: 1
                    type: string
                  uid:
                    description: |-
                      uid is the UID of the Preparation. A recreated Preparation with the
                      same name does not inherit votes.
                    type: string
                required:
                - artifactDigest
                - name
                - uid
                type: object
              submittedTime:
                description: |-
                  submittedTime is when the kokumi server accepted the vote. It orders
                  votes of the same approver submitted within the same second.
                format: date-time
                type: string
            required:
            - approver
            - decision
            - orderName
            - preparationRef
            - submittedTime
            type: object
            x-kubernetes-validations:
            - message: Approval spec is immutable
              rule: self == oldSelf
          status:
            description: status defines the observed state of Approval
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Approval resource.
                  The Counted condition reports whether this vote contributes to the
                  approval gate of its Preparation.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: observedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.orderName
    - jsonPath: .spec.preparationRef.name
    served: true
    storage: true
    subresources:
      status: {}
